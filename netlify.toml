# Netlify — 100% free static hosting, zero backend.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so any path serves the app shell.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
